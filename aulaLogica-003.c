#include <stdlib.h>
#include <stdio.h>
#include <windows.h>
#include <locale.h>
#include <math.h>

int main()
{
  setlocale(LC_ALL, "Portuguese");

  /*   float raio = 2.5, area = 3.14159 * pow(raio, 2), raio2 = raio;
    char sexo = 'F', digito = '5';

    int a = 3, b = 12, c_aux = b, delta = ((b * b) - 4) * (a * c_aux);

    printf("A = %i\nB = %i\n\n", c_aux, a);
   */
  /*   int a2 = 100, b2, total;
    printf("A%i / B%i = total%i", a2, b2, total); */

  // NÃO ESTA COMPLETO ......

  /*   int nota_200, nota_100, nota_50, nota_20, nota_10, nota_5, nota_2, valorSaque, restoNota_200, restoNota_100, restoNota_50, restoNota_20, restoNota_10, restoNota_5, restoNota_2;
    printf("Digite um valor para saque: ", valorSaque);
    scanf("%i", &valorSaque);

    if (valorSaque < 600)
    {
      }
    else
    {
      printf("Você deve digitar um valor menor que 600");
    }

    return 0;
  } */